{
    "watch": ["src"],
    "ext": "ts,json",
    "ignore": ["src/**/*.spec.ts"],
    "exec": "ts-node --project tsconfig.json -r tsconfig-paths/register src/app.ts -k node_modules internals .Trash-1000 lost+found -v"
}
